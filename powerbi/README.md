# Power BI workspace

Create the future `.pbip` project in this directory. Load the four CSV files from `data/processed` and relate each fact table to `dim_state` on `state_code` (one-to-many, single-direction filtering).

